// Package pfe2e содержит тесты, которые проверяют центральное утверждение
// splitr на настоящем ядре macOS: блокировка без quick держится загруженной
// постоянно, а живой якорь sshuttle перебивает её по правилу last-match-wins.
//
// В пакете нет прод-кода: файл существует только для того, чтобы пакет
// собирался на любой платформе (все тесты помечены `//go:build darwin`,
// а e2e — ещё и тегом pf_e2e). Иначе `go test ./...` в Linux-контейнере
// падал бы с «build constraints exclude all Go files».
package pfe2e
